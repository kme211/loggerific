extends layout

block content
  h1 Add session
  form(action="/add-session" method="POST")
    label(for="date") Date
    input(type="date" name="date" required)
    input(type="submit" value="Save →")